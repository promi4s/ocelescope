# @ocelescope/api-querying

Generated TypeScript client and TanStack Query hooks for the Ocelescope
querying backend module.

The package exposes `useOcelSchema` for field discovery and `useOcelQuery` for
bounded row and aggregate queries. It contains no visualization code.
