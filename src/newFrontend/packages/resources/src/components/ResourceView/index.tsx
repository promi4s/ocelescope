import type { VisulizationsType } from "../../types";

const VisualizationViewer: React.FC<{
  visualization: VisulizationsType;
  isPreview?: boolean;
}> = () => {
  return <></>;
};

export default VisualizationViewer;
