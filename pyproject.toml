[project]
name = "ocelescope-workspace"
version = "0.1.0"
description = "Workspace for ocelescope package"
readme = "README.md"
requires-python = ">=3.13"

[tool.uv.workspace]
members = ["src/backend", "src/ocelescope"]
